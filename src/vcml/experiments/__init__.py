# Intentionally empty and never edited.
#
# Experiments are discovered by scanning this package directory, so adding one
# touches only a NEW file. Nothing here is a shared edit point, which is what
# keeps parallel sessions free of merge conflicts.
